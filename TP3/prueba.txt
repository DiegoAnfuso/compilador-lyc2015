DECLARE
	(a:REAL, b:REAL, c:REAL, f:INT, j:INT, o:INT, k:INT, d:REAL)
ENDDECLARE
CONST p := 2.0;
CONST t := 20;
LET f: 2, k, b: 3.2, j, o:20 DEFAULT (5+2)*1;
PUT d;
b := a + 1;

WHILE(a*7+b < b+c*4 OR b < 0) 
	a := a + 1;
	a := 0.0;
	IF(a-8 < f*8+4 AND j*7 >= 88/2)
	THEN
		IF(c-8 < c+4 OR p*8 >= 2)
		THEN
			c := 1.2 + 1.5;
		ELSE
			k := t / 8;
		ENDIF
	ELSE
		d := d / 8 + b;
	ENDIF
	
END

--/
a1 := "abc";
a2 := "abc";
a3 := s2++s1;
a4 := (a + b) * 8 - c;/--

